<UserControl x:Class="RemotePythonExecutionTestApp.Modules.ModuleName.Views.ViewA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RemotePythonExecutionTestApp.Modules.ModuleName.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <Grid>
        <Grid.ColumnDefinitions>
            
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            
            <Button Grid.Row="0"
                Width="100"
                Height="20"
                Margin="20 0 10 0"
                HorizontalAlignment="Left"
                Content="Connect" 
                Command="{Binding ConnectCommand}"/>

            <Button Grid.Row="0"
                Width="100"
                Height="20"
                Margin="10 0 10 0"
                HorizontalAlignment="Left"
                Content="Interpret" 
                Command="{Binding InterpretCommand}"/>
            
        </StackPanel>

        <TextBox Grid.Row="1"
                 Margin="10"
                 FontSize="18"
                 Text="{Binding SourceCode, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch"
                 HorizontalContentAlignment="Left"
                 VerticalContentAlignment="Top"
                 />
    </Grid>
</UserControl>
